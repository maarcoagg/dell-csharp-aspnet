@model IEnumerable<Projetos.Models.Medicos>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodEspecialidadeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodEspecialidadeNavigation.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Crm">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Crm">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Crm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
